# AWS-Serverless-URL-Shortener
A serverless URL shortening application built on AWS using Lambda, API Gateway, and DynamoDB. The system generates unique short URLs, manages redirections, and provides scalable, cost-efficient URL management without server maintenance.
